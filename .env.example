# Root-level environment variables shared across the monorepo (local dev orchestration only).
# Per-app variables live in apps/web/.env.example and apps/api/.env.example.
# Copy this file to .env and fill in real values locally — never commit .env.

NODE_ENV=development
